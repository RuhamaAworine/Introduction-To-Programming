#include<stdio.h>
#include<string.h>
int main()
{
    char S[10000001];
    scanf("%s",&S);
    long long int count[];
    for(long long int i=0;i<strlen(S);i++)
    {
        count[i]=0;
    }
    for(long long i=0;i!='\0';i++)
    {
        long long int val=S[i];
        
    }
    return 0;
}